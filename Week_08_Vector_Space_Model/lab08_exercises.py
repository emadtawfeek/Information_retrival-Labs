"""Week 8: student starter. Finish TODOs before checking results."""
import os
import re
import math

BASE_DIR = os.path.dirname(os.path.abspath(__file__))
STOP_WORDS = {"a", "an", "the", "is", "and", "for", "with"}


def cosine_similarity(vector1, vector2):
    # TODO: Calculate dot product and magnitudes; guard zero vectors.
    return None


def main():
    print("Starter only: TODO functions are not completed yet.")
    print(cosine_similarity([1, 1], [1, 0]))


if __name__ == "__main__":
    main()
