"""Week 1: student starter. Finish TODOs before checking results."""
import os
import re
import math

BASE_DIR = os.path.dirname(os.path.abspath(__file__))
STOP_WORDS = {"a", "an", "the", "is", "and", "for", "with"}


def load_documents(folder_path):
    # TODO: Return a dictionary of IDs to text.
    return {}


def main():
    print("Starter only: TODO functions are not completed yet.")
    print(load_documents(os.path.join(BASE_DIR, "docs")))


if __name__ == "__main__":
    main()
